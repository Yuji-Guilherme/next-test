const wrapperClassName = 'h-screen';

const pageTransitionClassName =
  'w-full h-screen bg-zinc-700 top-0 left-0 absolute';

const originBottomClassName = `${pageTransitionClassName} origin-bottom`;

const originTopClassName = `${pageTransitionClassName} origin-top`;

export { wrapperClassName, originBottomClassName, originTopClassName };
